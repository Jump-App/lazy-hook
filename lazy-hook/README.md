# lazy-hook

Build the package:

```bash
bun install
bun run build
```

Create the local tarball consumed by the Phoenix example:

```bash
bun run pack:local
```

This writes `../lazy-hook-package.tgz`.
