{
  "name": "lazy-hook",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.10.6",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "phoenix_live_view": "1.2.3",
    "typescript": "^5"
  },
  "scripts": {
    "clean": "rm -rf ./dist && echo 'done.'",
    "check": "bun run --bun tsc",
    "build": "bun build ./index.ts --outdir ./dist --packages external",
    "watch": "bun run build -- --watch"
  }
}
